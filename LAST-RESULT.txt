TWO TASKS — Summary
======================

TASK 1: Fix broken campaign images
------------------------------------
Created: src/components/ui/campaign-image.tsx

CampaignImage component:
- Props: src (string|null), name (string), className (optional)
- If src is empty or image fails to load (onError): shows a placeholder div
  with the first letter of the campaign name, accent blue background, white text
- Uses React state (failed) to track load errors
- Prevents the broken image icon from ever showing

Replaced <img> tags in 5 files (7 locations total):
- src/app/(app)/campaigns/page.tsx (1 location)
- src/app/(app)/campaigns/[id]/page.tsx (1 location)
- src/app/(app)/favorites/page.tsx (1 location)
- src/app/(app)/admin/campaigns/page.tsx (2 locations: review section + grid)

All now show the placeholder even when imageUrl is empty (previously showed
nothing). Broken URLs now show the placeholder instead of the browser's
broken image icon.

TASK 2: Supabase Storage uploads
-----------------------------------
Installed: @supabase/supabase-js (^2.101.1)

Rewritten: src/app/api/upload/route.ts
- Removed: fs.writeFileSync, local /public/uploads/ storage
- Added: Supabase Storage client using NEXT_PUBLIC_SUPABASE_URL + ANON_KEY
- Upload flow:
  1. Validate file (type, size — same checks as before)
  2. Generate unique filename (timestamp + random)
  3. Upload to Supabase Storage bucket "uploads" via supabase.storage.upload()
  4. Get public URL via supabase.storage.getPublicUrl()
  5. Return the permanent Supabase URL
- If Supabase not configured: returns error "Storage not configured. Use an image URL instead."
- If upload fails: returns helpful error suggesting image URL alternative

MANUAL STEP REQUIRED:
Go to Supabase Dashboard → Storage → Create bucket "uploads" → Set to PUBLIC.
Without this, uploads will fail with a "bucket not found" error.

BUILD: ✓ (prisma generate + next build, 33/33 pages)
