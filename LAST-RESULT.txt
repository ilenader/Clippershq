FOUR TASKS — Summary
======================

TASK 1: Cron to hourly
  File: vercel.json
  Changed: "0 0 * * *" → "0 * * * *" (every hour on the hour, Vercel Pro)

TASK 2: Delete debug endpoints
  Deleted: src/app/api/debug/ (entire directory)
  - /api/debug/auth-check (temporary env diagnostic)
  - /api/debug/session (OWNER-only session viewer)
  Both removed — no longer needed.

TASK 3: Image URL input for campaigns
  File: src/app/(app)/admin/campaigns/page.tsx
  Added text input below the ImageUpload component:
    Label: "Or paste an image URL"
    Placeholder: "https://example.com/image.jpg"
    Bound to form.imageUrl (same field as the upload)
  If user pastes a URL, it's stored directly as imageUrl.
  If they upload a file, the upload component sets imageUrl.
  If both: the text input shows the current value either way.
  This works on Vercel (no filesystem needed for URL-based images).

TASK 4: Production safety audit
  Verified all guards are in place:

  ✓ /api/dev-auth POST+GET: returns 404 when NODE_ENV=production
  ✓ /api/dev/reset-campaigns: returns 403 when NODE_ENV=production
  ✓ /api/dev/run-tracking: returns 403 when NODE_ENV=production
  ✓ /api/cron/tracking: checks Bearer CRON_SECRET in production
  ✓ isDevBypassEnabled(): requires NODE_ENV !== "production" AND DEV_AUTH_BYPASS=true
  ✓ /dev-login page: shows "not enabled" when isDevMode is false
  ✓ No console.log statements leak secrets/tokens/passwords
  ✓ Operational logs ([TRACKING], [EMAIL], [Clip Submit]) kept — no sensitive data
  ✓ Debug endpoints now deleted (Task 2)

BUILD: ✓ (prisma generate + next build, 33/33 pages)
