# Pin Node to 20 LTS. Next.js 16 requires Node >= 20; without this pin the
# default Nixpacks Node version can drift and break the build on Railway.
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
